export function AnimationTest() {
  return (
    <div id="animation-test-root" style={{ minHeight: "100vh" }}>
      {/* Drop your GSAP animation markup/scripts here. No site styles applied. */}
    </div>
  );
}
